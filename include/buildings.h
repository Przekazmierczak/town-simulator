int build_a_building(struct Town *town, int next_building);
void maintain_buildings(struct Town *town);
float fire_department(int resident_per_fire_department);
float hospital(int resident_per_hospitel);
float school(int resident_per_school);